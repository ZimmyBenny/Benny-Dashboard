---
phase: quick-260415-0a3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/dj.pdf.service.ts
  - backend/src/routes/dj.quotes.routes.ts
  - frontend/src/pages/dj/DjQuoteDetailPage.tsx
autonomous: false
requirements: [PDF-PREVIEW]
must_haves:
  truths:
    - "Benutzer kann im Angebot-Detail 'Vorschau' klicken und sieht ein korrektes DIN-A4-PDF im neuen Tab"
    - "PDF enthaelt Absenderzeile, Empfaenger, Meta-Block, Positionstabelle, Summen und Footer gemaess RECHNUNGS_TEMPLATE.md"
    - "Firmendaten werden aus dj_settings DB geladen, nicht hardcoded"
    - "Button ist nur bei gespeicherten Angeboten aktiv"
  artifacts:
    - path: "backend/src/services/dj.pdf.service.ts"
      provides: "pdfkit-basierter Angebot-PDF-Generator"
    - path: "backend/src/routes/dj.quotes.routes.ts"
      provides: "GET /api/dj/quotes/:id/preview Endpunkt"
    - path: "frontend/src/pages/dj/DjQuoteDetailPage.tsx"
      provides: "Vorschau-Button im Header und Footer"
  key_links:
    - from: "frontend/src/pages/dj/DjQuoteDetailPage.tsx"
      to: "/api/dj/quotes/:id/preview"
      via: "window.open in neuem Tab"
    - from: "backend/src/routes/dj.quotes.routes.ts"
      to: "backend/src/services/dj.pdf.service.ts"
      via: "generateQuotePdf() Aufruf"
    - from: "backend/src/services/dj.pdf.service.ts"
      to: "dj_settings (key='company')"
      via: "db.prepare SELECT"
---

<objective>
Implementiere eine vollstaendige Angebot-PDF-Vorschau mit pdfkit.

Purpose: DJ-Angebote als professionelle PDFs im Browser anzeigen, basierend auf dem
RECHNUNGS_TEMPLATE.md Layout (DIN A4, schwarz auf weiss, Geschaeftsbrief-Stil).

Output: Funktionierender PDF-Generator + Preview-Endpunkt + Vorschau-Button im UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@DJ Dashboard/files/RECHNUNGS_TEMPLATE.md
@backend/src/services/dj.pdf.service.ts
@backend/src/routes/dj.quotes.routes.ts
@frontend/src/pages/dj/DjQuoteDetailPage.tsx
@frontend/src/api/dj.api.ts (DjQuote, DjQuoteItem, DjCompanySettings Typen)
@backend/src/db/migrations/015_contacts.sql (contact_addresses Tabelle fuer Empfaenger-Adresse)
@backend/src/db/migrations/026_dj_schema.sql (dj_settings Tabelle, company JSON-Struktur)

<interfaces>
<!-- Firmendaten aus dj_settings key='company' (JSON): -->
{
  "name": "Benjamin Zimmermann",
  "company": "Dein Event DJ | Benjamin Zimmermann",
  "address": "Mittelweg 10",
  "zip": "93426",
  "city": "Roding",
  "country": "Deutschland",
  "phone": "01711493222",
  "email": "Benjamin.Z@gmx.de",
  "website": "www.dein-event-dj.com",
  "tax_number": "21129292323",
  "vat_id": null,
  "is_vat_liable": true,
  "vat_rate": 19.0,
  "bank": {
    "name": "Raiffeisenbank Neustadt - Vohenstrau\u00df eG",
    "iban": "DE59753900000005302552",
    "bic": "GENODEF1NEW",
    "holder": "Benjamin Zimmermann"
  }
}

<!-- Kontakt-Adresse aus contact_addresses Tabelle: -->
contact_addresses: id, contact_id, street, postal_code, city, country, label, is_primary

<!-- DjQuoteItem Felder: -->
position, description, quantity, unit, price_net, tax_rate, discount_pct, total_net

<!-- DjQuote Felder: -->
id, number, customer_id, event_id, subject, status, quote_date, valid_until,
subtotal_net, tax_total, total_gross, header_text, footer_text, finalized_at
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pdfkit Angebot-PDF-Generator in dj.pdf.service.ts</name>
  <files>backend/src/services/dj.pdf.service.ts</files>
  <action>
Ersetze den Platzhalter in `generateQuotePdf` durch eine vollstaendige pdfkit-Implementierung.
Behalte `generateInvoicePdf` als Platzhalter (wird spaeter implementiert).

**Neue Funktion `generateQuotePreviewPdf(quoteId: number): Promise<Buffer>`:**

1. **Daten laden aus DB** (import db from '../db/connection'):
   - Quote: `SELECT * FROM dj_quotes WHERE id = ? AND deleted_at IS NULL`
   - Items: `SELECT * FROM dj_quote_items WHERE quote_id = ? ORDER BY position`
   - Kunde: `SELECT * FROM contacts WHERE id = ?` (customer_id aus Quote)
   - Kunde-Adresse: `SELECT * FROM contact_addresses WHERE contact_id = ? AND is_primary = 1` (Fallback: erste Adresse ohne is_primary-Filter)
   - Firmendaten: `SELECT value FROM dj_settings WHERE key = 'company'` — JSON.parse
   - Event (optional): `SELECT title, event_date FROM dj_events WHERE id = ?` wenn event_id gesetzt

2. **PDF erstellen mit pdfkit** (DIN A4: 595.28 x 841.89 pt):
   - Margins: top 71pt (25mm), bottom 71pt, left 71pt (25mm), right 57pt (20mm)
   - Schrift: Helvetica (eingebaut in pdfkit, kein externer Font noetig)

3. **Layout gemaess RECHNUNGS_TEMPLATE.md Abschnitt "Angebot-PDF":**

   a) **Absenderzeile** (y ~ 71pt):
      - `${company.name} · ${company.address} · ${company.zip} ${company.city}`
      - 8pt, Farbe #666666, danach 0.5pt Linie darunter

   b) **Empfaenger-Block** (links, ab y ~ 142pt / 50mm):
      - Anrede, Vorname Nachname (oder organization_name bei contact_kind='organization')
      - Firma (falls Person mit organization_name)
      - Strasse aus contact_addresses
      - PLZ + Ort aus contact_addresses
      - 11pt, schwarz

   c) **Meta-Block** (rechts, gleiche Y-Hoehe wie Empfaenger, bei x ~ 350pt):
      - Label: Wert Paare, 10pt
      - "Angebots-Nr.:" → quote.number ?? "Entwurf"
      - "Angebotsdatum:" → quote.quote_date (deutsch formatiert DD.MM.YYYY)
      - "Gueltig bis:" → quote.valid_until (deutsch formatiert)
      - "Referenz:" → event.title (wenn vorhanden)
      - "Kundennummer:" → customer.customer_number ?? "-"

   d) **Titelzeile** (ca. 20mm unter Meta-Block Ende):
      - `Angebot Nr. ${quote.number}` oder `Angebot (Entwurf)` wenn kein number
      - 14pt, bold (font: 'Helvetica-Bold')

   e) **Kopftext** (wenn quote.header_text vorhanden):
      - 10pt normal, Zeilenabstand 1.4 (lineGap ~4pt)
      - Platzhalter ersetzen: {{vorname}} → customer.first_name, {{nachname}} → customer.last_name,
        {{anrede}} → customer.salutation, {{eventdatum}} → event.event_date formatiert

   f) **Positionstabelle:**
      - Header-Zeile: 9pt bold, Hintergrund #F0F0F0
      - Spalten (Breiten relativ zur Seitenbreite 467pt):
        Pos (8%) | Beschreibung (45%) | Menge (10%) | Einheit (12%) | Einzelpreis netto (12%) | Gesamt netto (13%)
      - Datenzeilen: 10pt, Trennlinien 0.5pt #CCCCCC
      - Preise: deutsche Formatierung mit Hilfsfunktion `formatEur(n)` → "1.234,56 EUR"
      - Ausrichtung: Pos=Mitte, Beschreibung=links, Menge=rechts, Einheit=Mitte, Preise=rechts

   g) **Summen-Block** (rechtbuendig unter Tabelle):
      - "Nettosumme: {subtotal_net}"
      - MwSt gruppiert nach tax_rate: Items nach tax_rate gruppieren, je Gruppe
        "Umsatzsteuer {rate} %: {summe}" — Summe = sum(item.total_net * item.tax_rate / 100) pro Gruppe
      - Trennlinie 0.5pt
      - "Angebotssumme brutto: {total_gross}" — 11pt bold

   h) **Fusstext:**
      - quote.footer_text wenn vorhanden, sonst Standard:
        "Dieses Angebot ist freibleibend und gueltig bis {valid_until}."
      - Platzhalter ersetzen: {{gueltig_bis}} → valid_until formatiert
      - "Mit freundlichen Gruessen" + Leerzeile + company.name

   i) **Footer** (fix positioniert am Seitenende, 25mm vom unteren Rand):
      - 1pt graue Linie (#CCCCCC)
      - 4-spaltige Fusszeile, 8pt, Farbe #666666:
        Spalte 1: company.company + "\n" + company.name + "\n" + company.address + "\n" + company.zip + " " + company.city
        Spalte 2: "Tel: " + company.phone + "\n" + company.email + "\n" + company.website
        Spalte 3: "Steuer-Nr: " + company.tax_number
        Spalte 4: company.bank.name + "\n" + "IBAN: " + company.bank.iban + "\n" + "BIC: " + company.bank.bic

4. **Hilfsfunktionen im selben File:**
   - `formatEur(n: number): string` → deutsches Format: `new Intl.NumberFormat('de-DE', { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(n) + ' EUR'`
   - `formatDateDE(isoStr: string): string` → DD.MM.YYYY
   - `replacePlaceholders(text: string, vars: Record<string, string>): string` → {{key}} ersetzen

5. **Buffer-Rueckgabe:** PDF in einen Buffer sammeln mit doc.on('data') chunks + doc.on('end'), KEIN Speichern auf Disk bei Preview. Nutze folgendes Pattern:
   ```
   return new Promise<Buffer>((resolve, reject) => {
     const chunks: Buffer[] = [];
     doc.on('data', (chunk: Buffer) => chunks.push(chunk));
     doc.on('end', () => resolve(Buffer.concat(chunks)));
     doc.on('error', reject);
     // ... PDF content ...
     doc.end();
   });
   ```

6. **Seitenumbruch-Handling:** Vor der Positionstabelle pruefen ob genug Platz.
   Bei Footer: als `doc.on('pageAdded')` listener den Footer auf jeder neuen Seite rendern.
   WICHTIG: Footer-Bereich (letzte 80pt) freihalten — setze doc.page.margins.bottom entsprechend
   hoeher (ca. 120pt statt 71pt), damit pdfkit automatisch umbricht bevor der Footer-Bereich erreicht wird.
  </action>
  <verify>
    <automated>cd "/Users/benny/Library/Mobile Documents/com~apple~CloudDocs/B E N N Y 👨🏽‍💻/09 - Benny Dashboard/backend" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - dj.pdf.service.ts enthaelt vollstaendige generateQuotePreviewPdf() Funktion
    - Alle Firmendaten aus DB, nicht hardcoded
    - Layout entspricht RECHNUNGS_TEMPLATE.md (Absender, Empfaenger, Meta, Tabelle, Summen, Footer)
    - TypeScript kompiliert ohne Fehler
  </done>
</task>

<task type="auto">
  <name>Task 2: Preview-Endpunkt + Vorschau-Button</name>
  <files>backend/src/routes/dj.quotes.routes.ts, frontend/src/pages/dj/DjQuoteDetailPage.tsx</files>
  <action>
**Backend — neuer Endpunkt in dj.quotes.routes.ts:**

1. Import hinzufuegen: `import { generateQuotePreviewPdf } from '../services/dj.pdf.service';`

2. Neuer GET-Endpunkt NACH dem bestehenden `GET /:id` und VOR `POST /` (damit `:id/preview` nicht mit POST kollidiert):

```typescript
// GET /api/dj/quotes/:id/preview — PDF-Vorschau
router.get('/:id/preview', async (req, res) => {
  const id = Number(req.params.id);
  if (!Number.isInteger(id)) { res.status(400).json({ error: 'Ungueltige ID' }); return; }

  const quote = db.prepare('SELECT * FROM dj_quotes WHERE id = ? AND deleted_at IS NULL').get(id);
  if (!quote) { res.status(404).json({ error: 'Angebot nicht gefunden' }); return; }

  const pdfBuffer = await generateQuotePreviewPdf(id);
  const displayNumber = (quote as { number: string | null }).number ?? 'Entwurf';
  res.setHeader('Content-Type', 'application/pdf');
  res.setHeader('Content-Disposition', `inline; filename="Angebot-${displayNumber}.pdf"`);
  res.send(pdfBuffer);
});
```

3. WICHTIG: Express 5 propagiert async Fehler automatisch — kein try/catch noetig.

**Frontend — Vorschau-Button in DjQuoteDetailPage.tsx:**

1. Im oberen Button-Bereich (vor dem Speichern-Button, ca. Zeile 547) einen neuen Button einfuegen:

```tsx
{!isNew && (
  <button
    type="button"
    style={btnSecondary}
    onClick={() => window.open(`/api/dj/quotes/${id}/preview`, '_blank')}
    title="PDF-Vorschau im neuen Tab oeffnen"
  >
    <span className="material-symbols-outlined" style={{ fontSize: '1rem' }}>picture_as_pdf</span>
    Vorschau
  </button>
)}
```

2. `isNew` erkennen: existiert bereits als Bedingung im Code — wenn `!params.id` oder aehnlich.
   Pruefe wie die Seite `isNew` bestimmt (wahrscheinlich `const isNew = !params.id || params.id === 'new'`).
   Finde diese Variable und nutze sie fuer die Sichtbarkeit.

3. Auch im unteren Button-Bereich (Zeile 1033-1046) den gleichen Vorschau-Button hinzufuegen,
   zwischen Abbrechen und Speichern.

4. KEIN neuer API-Call noetig — window.open nutzt den Browser-nativen PDF-Viewer.
   Die URL `/api/dj/quotes/${id}/preview` geht ueber den Vite-Proxy (baseURL '/api' Pattern).
   Allerdings window.open braucht den vollen relativen Pfad, also checken ob der Vite-Proxy-Prefix
   passt. Schaue in vite.config.ts wie der Proxy konfiguriert ist. Wahrscheinlich ist
   `/api` → `http://localhost:3001` gemappt, also funktioniert `/api/dj/quotes/${id}/preview`.
  </action>
  <verify>
    <automated>cd "/Users/benny/Library/Mobile Documents/com~apple~CloudDocs/B E N N Y 👨🏽‍💻/09 - Benny Dashboard/backend" && npx tsc --noEmit 2>&1 | head -20 && echo "--- Frontend ---" && cd "/Users/benny/Library/Mobile Documents/com~apple~CloudDocs/B E N N Y 👨🏽‍💻/09 - Benny Dashboard/frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - GET /api/dj/quotes/:id/preview gibt Content-Type application/pdf zurueck
    - Vorschau-Button im Header und Footer des Angebot-Formulars sichtbar (nur bei gespeichertem Angebot)
    - Klick oeffnet PDF im neuen Browser-Tab
    - TypeScript kompiliert ohne Fehler in beiden Projekten
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Verifikation der PDF-Vorschau</name>
  <action>Benutzer prueft PDF-Layout und Button-Verhalten manuell.</action>
  <what-built>
    Angebot-PDF-Vorschau: pdfkit-Generator nach RECHNUNGS_TEMPLATE.md + Preview-Endpunkt + Button im UI.
  </what-built>
  <how-to-verify>
    1. Backend + Frontend starten (falls nicht laufend)
    2. Im DJ-Bereich ein bestehendes Angebot oeffnen (oder neues erstellen und speichern)
    3. "Vorschau"-Button klicken — neuer Tab mit PDF sollte sich oeffnen
    4. PDF pruefen:
       - Absenderzeile oben korrekt (Benjamin Zimmermann, Mittelweg 10, 93426 Roding)
       - Empfaenger-Block mit Kundendaten (Name, Adresse)
       - Meta-Block rechts: Angebots-Nr/Entwurf, Datum, Gueltig bis, ggf. Referenz, Kundennummer
       - Titel: "Angebot Nr. AN-XXXX" oder "Angebot (Entwurf)"
       - Positionstabelle mit korrekten Spalten und deutschen Preisformaten
       - Summen-Block: Netto, MwSt, Brutto
       - Footer mit 4-spaltiger Fusszeile (Firmendaten, Kontakt, Steuer, Bank)
    5. Vorschau-Button sollte bei "Neues Angebot" (noch nicht gespeichert) NICHT sichtbar sein
  </how-to-verify>
  <resume-signal>"approved" oder Aenderungswuensche beschreiben</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Preview-Endpunkt | Quote-ID aus URL-Parameter, koennte manipuliert sein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0a3-01 | I (Information Disclosure) | GET /preview | accept | Alle Daten gehoeren dem einzigen Benutzer; Auth-Middleware auf DJ-Router bereits aktiv |
| T-0a3-02 | T (Tampering) | :id Parameter | mitigate | parseInt + isInteger Validierung vor DB-Query |
| T-0a3-03 | D (Denial of Service) | PDF-Generierung | accept | Lokale Single-User-App, kein oeffentliches Internet |
</threat_model>

<verification>
1. TypeScript kompiliert fehlerfrei in Backend und Frontend
2. GET /api/dj/quotes/:id/preview gibt application/pdf zurueck
3. PDF enthaelt alle Pflichtbereiche des RECHNUNGS_TEMPLATE.md
4. Vorschau-Button nur bei gespeicherten Angeboten sichtbar
</verification>

<success_criteria>
- PDF oeffnet sich im neuen Tab mit korrektem Layout
- Firmendaten aus DB, nicht hardcoded
- Deutsche Preisformatierung (1.234,56 EUR)
- Datumsformatierung DD.MM.YYYY
- Button-Sichtbarkeit korrekt (nur bei vorhandener ID)
</success_criteria>

<output>
After completion, create `.planning/quick/260415-0a3-dj-angebot-pdf-vorschau-implementieren-p/260415-0a3-SUMMARY.md`
</output>
