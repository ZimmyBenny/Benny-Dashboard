import { PageWrapper } from '../components/layout/PageWrapper';

// Temporaerer Stub — wird in Plan 05 vollstaendig ueberschrieben
export function DashboardPage() {
  return (
    <PageWrapper>
      <p style={{ color: 'var(--color-on-surface)' }}>Dashboard wird geladen...</p>
    </PageWrapper>
  );
}
