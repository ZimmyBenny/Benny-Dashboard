import { PageWrapper } from '../components/layout/PageWrapper';

// Temporaerer Stub — wird in Plan 05 vollstaendig ueberschrieben
export function SettingsPage() {
  return (
    <PageWrapper>
      <p style={{ color: 'var(--color-on-surface)' }}>Einstellungen werden geladen...</p>
    </PageWrapper>
  );
}
